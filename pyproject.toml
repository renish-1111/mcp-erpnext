[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai_mcp"
version = "0.0.1"
description = "Universal Dynamic MCP Server for Frappe / ERPNext"
authors = [{name = "ERPNext AI Team"}]

[project.urls]
Home = "https://github.com/frappe/mcp"
